const slowa = ["Jablko", "Gruczka", "Banan", "Wisnia"];

slowa.sort();

console.log(slowa);

const liczby = [21, 21, 2, 12, 3, 5, 3, 42, 5, 2, 4, 241];

liczby.sort((a, b) => a - b);

console.log(liczby);

